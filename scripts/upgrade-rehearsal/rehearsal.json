{
  "$comment": "Release-only upgrade rehearsal (docs/operations/upgrades.md#release-gate-upgrade-rehearsal). For every supported source release, the candidate upgrades one install per dataset. Datasets are generated by the SOURCE release's own tooling, because a real install holds data its own version wrote. Every class in requiredDatasetClasses must name at least one dataset, and plan.mjs refuses otherwise. Never wire this into push or pull_request CI.",
  "sources": [
    { "tag": "v0.1.0-alpha.22" },
    { "tag": "v0.1.0-alpha.23" }
  ],
  "requiredDatasetClasses": [
    "empty",
    "small",
    "perf-1m",
    "multi-entity",
    "samples",
    "simulation",
    "edge"
  ],
  "datasets": [
    {
      "id": "empty",
      "class": "empty",
      "description": "A bootstrapped install with no activity: the default org only.",
      "steps": []
    },
    {
      "id": "small",
      "class": "small",
      "description": "One general contractor, one quarter of seeded activity.",
      "steps": [
        { "kind": "sim", "mode": "run", "profile": "general-contractor", "seed": "upgrade-small-1", "start": "2026-01-01", "end": "2026-03-31" }
      ]
    },
    {
      "id": "samples",
      "class": "samples",
      "description": "Every sample-company template the source release can prepare.",
      "steps": [
        { "kind": "samples" }
      ]
    },
    {
      "id": "simulation",
      "class": "simulation",
      "description": "Recurring-revenue and services companies over a fiscal year-end.",
      "steps": [
        { "kind": "sim", "mode": "run", "profile": "saas", "seed": "upgrade-sim-1", "start": "2026-07-01", "end": "2027-02-28" },
        { "kind": "sim", "mode": "run", "profile": "professional-services", "seed": "upgrade-sim-2", "start": "2026-10-01", "end": "2027-03-31" }
      ]
    },
    {
      "id": "edge",
      "class": "edge",
      "description": "Endurance loop across a year-end and a leap day, with the adversarial probes: backdating into closed periods, posted edits, over-application, reversal symmetry, void and recreate.",
      "steps": [
        { "kind": "sim", "mode": "endurance", "profile": "general-business", "seed": "upgrade-edge-1", "start": "2027-11-01", "end": "2028-03-31" }
      ]
    }
  ]
}
