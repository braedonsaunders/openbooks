import { SplashHold } from '@/components/brand-splash'

/** App-wide route loading: keep the brand splash up while the shell streams. */
export default function AppLoading() {
  return <SplashHold />
}
